build:
  docker:
    web: ./front-end
    api: ./back-end
    nginx: ./nginx

run:
  web:
    command: npm start
    detach: false

  api:
    command: npm start
    detach: false
    environment:
      - NODE_ENV=production

  nginx:
    command: nginx -g "daemon off;"
    detach: false
    ports:
      - "8000:80"

addons:
  - plan: heroku-postgresql
    as: DATABASE
